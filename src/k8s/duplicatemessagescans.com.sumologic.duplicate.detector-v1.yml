# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: duplicatemessagescans.com.sumologic.duplicate.detector
spec:
  group: com.sumologic.duplicate.detector
  names:
    kind: DuplicateMessageScan
    plural: duplicatemessagescans
    shortNames:
    - dms
    singular: duplicatemessagescan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.endTime
      name: END_TIME
      priority: 0
      type: string
    - jsonPath: .spec.startTime
      name: START_TIME
      priority: 0
      type: string
    - jsonPath: .status.completedSegmentCount
      name: COMPLETED_SEGMENTS
      priority: 1
      type: integer
    - jsonPath: .status.error
      name: ERROR
      priority: 0
      type: string
    - jsonPath: .status.failed
      name: FAILED
      priority: 0
      type: boolean
    - jsonPath: .status.failedSegmentCount
      name: FAILED_SEGMENTS
      priority: 1
      type: integer
    - jsonPath: .status.pendingSegmentCount
      name: PENDING_SEGMENTS
      priority: 1
      type: integer
    - jsonPath: .status.processingSegmentCount
      name: PROCESSING_SEGMENTS
      priority: 1
      type: integer
    - jsonPath: .status.segmentCount
      name: SEGMENTS
      priority: 0
      type: integer
    - jsonPath: .status.successful
      name: SUCCESSFUL
      priority: 0
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              customers:
                description: List of customers to scan
                items:
                  type: string
                type: array
              endTime:
                description: "End time for the scan, in ISO format"
                type: string
              maxParallelScans:
                default: 1
                description: Max number of scans that can be performed at the same
                  time
                minimum: 1.0
                type: integer
              retriesPerSegment:
                default: 3
                description: How many times to attempt each segment
                maximum: 5.0
                minimum: 1.0
                type: integer
              startTime:
                description: "Start time for the scan, in ISO format"
                type: string
              targetObject:
                default: indices
                description: What to scan either blocks or indices
                type: string
              timeRangeSegmentLength:
                description: "Length of how long each scan should be, for example\
                  \ to break the full time range into 5m segments set this to PT5m"
                nullable: true
                type: string
              volumeSize:
                description: Size of the volume to attach to the scanning jobs
                type: string
            required:
            - startTime
            - endTime
            - customers
            type: object
          status:
            properties:
              completedSegmentCount:
                type: integer
              completionTime:
                type: string
              error:
                type: string
              failed:
                type: boolean
              failedSegmentCount:
                type: integer
              lastUpdateTime:
                type: string
              observedGeneration:
                type: integer
              pendingSegmentCount:
                type: integer
              processingSegmentCount:
                type: integer
              scanStartedTime:
                type: string
              segmentCount:
                type: integer
              segments:
                description: Pairs of time range and customer that will be scanned
                  independently
                items:
                  properties:
                    customer:
                      type: string
                    endTime:
                      type: string
                    id:
                      type: string
                    startTime:
                      type: string
                    status:
                      enum:
                      - COMPLETED
                      - FAILED
                      - PENDING
                      - PROCESSING
                      type: string
                  type: object
                type: array
              successful:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
